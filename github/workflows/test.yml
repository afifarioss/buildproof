name: Removed legacy workflow

on:
  workflow_dispatch: {}

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: No-op
        run: echo "This workflow has been removed; the active workflow is at .github/workflows/test.yml"
